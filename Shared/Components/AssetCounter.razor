<div class="alert alert-secondary mt-4">
    <span class="oi oi-pencil me-2" aria-hidden="true">@detectedDevices devices found out of @totalDevices total devices</span>
</div>


@code {
    private int totalDevices = DeviceHandler.getTotalDeviceAmount();
    public int detectedDevices = DeviceHandler.getCurrentDevices();
}
